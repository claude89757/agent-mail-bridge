/**
 * Library entry point for agent-mail-bridge.
 *
 * Phase 0 skeleton: real functionality arrives module by module
 * (domain, transports, drivers, application, store, daemon, cli).
 */

export const PACKAGE_NAME = 'agent-mail-bridge';
